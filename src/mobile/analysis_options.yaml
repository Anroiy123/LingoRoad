include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_single_quotes: true
    use_super_parameters: true
    # Public constructor arguments intentionally map to private fields.
    prefer_initializing_formals: false
