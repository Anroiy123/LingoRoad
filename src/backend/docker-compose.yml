services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: lingoroad
      POSTGRES_USER: lingoroad
      POSTGRES_PASSWORD: lingoroad
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
