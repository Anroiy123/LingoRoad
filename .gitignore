# JetBrains IDE config
.idea/

.agents
.codex
# This machine's global gitignore excludes .claude/ everywhere; this repo
# tracks it deliberately (Claude Code context docs), except the local
# settings file below, which can carry machine-specific secrets.
!/.claude/
/.claude/settings.local.json
context.md
/docs/superpowers/

# Android signing material
src/mobile/android/key.properties
src/mobile/android/app/*.jks
src/mobile/android/app/*.keystore
deploy/.env.production
